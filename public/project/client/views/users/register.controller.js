(function () {
    'use strict';

    angular
        .module('FlairdropApp')
        .controller('LoginController', LoginController);

    function LoginController(UserService) {
        var model = this;
        
    }
}());
